{
	"name": "@bun-and-butter/error",
	"type": "module",
	"version": "1.0.0",
	"exports": {
		".": "./src/error.ts"
	},
	"description": "A tiny foundation for strongly typed domain errors in TypeScript. Define consistent error classes with machine-readable codes, default messages, optional causes, and structured metadata. It works well for application domains that need predictable error handling, clear diagnostics, and a small, reusable error surface.",
	"keywords": [
		"error",
		"domain-error",
		"typed-error"
	],
	"homepage": "https://bun-and-butter.dev/package/error",
	"bugs": "https://github.com/bun-and-butter/error/issues",
	"repository": "github:bun-and-butter/error",
	"license": "Apache-2.0",
	"scripts": {
		"test": "bun test",
		"test:coverage": "bun test --coverage",
		"test:lint": "biome lint",
		"test:format": "biome format",
		"test:types": "tsc --noEmit",
		"lint": "biome lint",
		"lint:fix": "biome lint --fix",
		"format": "biome format",
		"format:fix": "biome format --write"
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.10",
		"@types/bun": "1.3.11",
		"typescript-result": "3.5.2",
		"typescript": "^6.0.2"
	}
}
